import { MinNumValidatorDirective } from './min-num-validator.directive';

describe('MinNumValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MinNumValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
